# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
env/
*.egg-info/
dist/
build/

# Jupyter
.ipynb_checkpoints/
*.ipynb_checkpoints

# Dados pesados (disponibilizar externamente se necessário)
data/*_raw.jsonl
# corpus.jsonl é entregue no ZIP (não ignorar)

# macOS
.DS_Store
__MACOSX/

# IDEs
.vscode/
.idea/

# LaTeX build artifacts (fontes ficam em relatorio-fonte/; relatorio.pdf na raiz É entregue)
relatorio-fonte/*.aux
relatorio-fonte/*.log
relatorio-fonte/*.out
relatorio-fonte/*.toc
relatorio-fonte/*.synctex.gz
relatorio-fonte/*.bbl
relatorio-fonte/*.blg
relatorio-fonte/*.bcf
relatorio-fonte/*.run.xml
relatorio-fonte/*.fdb_latexmk
relatorio-fonte/*.fls
relatorio-fonte/relatorio.pdf

# Conteúdo NÃO público
# roteiro interno de gravação
roteiro-video.md
# material do docente (não redistribuir)
Material_Apoio_T1/
# enunciado do docente
Trabalho_2026-1.pdf
# contém IPs/infra do homelab — não publicar
docker/DEPLOY-*.md

# Segredos
.env
*.key
*.pem
id_*

# Submissao (nao publicar): contem enderecos/credenciais de acesso
LINKS.txt

# pytest
.pytest_cache/

# Embeddings cache (regeneravel)
*.npy

# Material interno de processo (nao publicar)
_interno/
# Arquivos de trabalho da anotacao (o entregavel e o qrels.tsv final)
eval/pool_revisao.tsv
eval/pool_novos_para_anotar.tsv
eval/qrels_sugerido.tsv

# Backup do corpus pre-arXiv (regeneravel via merge_corpus.py)
data/corpus_pre_arxiv.jsonl
